<% content_for(:title, @sauna.name) %>
<div class="w-5/6 mx-auto my-24 max-w-screen-xl">
  <h1 class="font-bold text-3xl mb-10"><%= @sauna.name %></h1>

  <div class="flex justify-center md:flex flex-wrap">
    <% if @posts.present? %>
      <%= render @posts %>
    <% else %>
      <div>
        <p>まだサ飯の投稿がないよ！</p>
      </div>
    <% end %>
  </div>
  <div>
    <!-- <%= paginate @posts %> -->
  </div>
</div>