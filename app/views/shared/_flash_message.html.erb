<% flash.each do |message_type, message| %>
  <div class="<%= flash_background_color(message_type) %> my-24 w-5/6 mx-auto p-4 rounded-md text-white max-w-xs md:max-w-4xl md:text-center">
    <%= message %>
  </div>
<% end %>